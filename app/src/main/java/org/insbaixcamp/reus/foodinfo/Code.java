package org.insbaixcamp.reus.foodinfo;

import java.util.ArrayList;
import java.util.List;

public class Code {
    /*private static Code instance;
    private List<String> barcode;

    private Code() {
        barcode = new ArrayList<>();
    }

    public static synchronized Code getInstance() {
        if (instance == null) {
            instance = new Code();
        }
        return instance;
    }

    public Code(List<String> barcode) {

        this.barcode = barcode;
    }


    public void addCode(String code) {
        // Verificar si el producto ya existe en la lista

        for (String valor : barcode) {
            if (valor.equals(code)) {
                return;
            }
        }
        // El producto no existe, se agrega a la lista
        barcode.add(code);
    }*/
}